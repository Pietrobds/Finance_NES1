from setuptools import setup, find_packages

setup(
    name="Finances_NES",
    version="0.01",
    description="library to work with finance, being able to make transactions, investments and create accounts",
    install_requires=[],
    packages=find_packages(),
)